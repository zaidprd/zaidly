---
// src/components/section/CoffeeGearHub.astro
interface Post {
  slug: string;
  data: {
    title: string;
    image?: string;
  };
}

interface Props {
  posts: Post[];
}

const { posts } = Astro.props;

const gearFeaturedLinks = [
  { 
    title: "Best Espresso Machines", 
    url: "/gear/best-espresso-machines", 
    image: "/images/BestEspressoMachines.webp" 
  },
  { 
    title: "Top Coffee Grinders", 
    url: "/gear/best-coffee-grinders", 
    image: "/images/TopCoffeeGrinders.webp" 
  },
  { 
    title: "Pour Over Essentials", 
    url: "/gear/pour-over-guide", 
    image: "/images/PourOverEssentials.webp" 
  },
  { 
    title: "Maintenance Tools", 
    url: "/gear/cleaning-maintenance", 
    image: "/images/MaintenanceTool.webp" 
  },
];
---

<section class="w-full bg-coffee-light pt-16 pb-8 border-t border-coffee-brown/10">
  
  <div class="container mx-auto max-w-6xl px-6 md:px-12">

    <h2 class="text-4xl md:text-5xl font-black text-coffee-dark mb-6 tracking-tight">
      Coffee Gear Authority
    </h2>

    <p class="text-lg md:text-xl text-coffee-brown mb-10 leading-relaxed max-w-3xl">
      Investing in the right tools is the secret to cafe-quality coffee at home. We rigorously test grinders, brewers, and machines to ensure you get the best performance for your budget.
    </p>

    <div class="mb-14">
      <p class="font-bold text-coffee-dark mb-5 text-xl">In our coffee gear hub, you’ll learn about:</p>
      <ul class="grid grid-cols-1 md:grid-cols-2 gap-x-12 gap-y-4 text-coffee-brown text-lg">
        <li class="flex items-start gap-x-3">
          <span class="text-accent-primary mt-1">⚙️</span>
          <span><strong class="text-coffee-dark">Precision:</strong>&nbsp;Choosing grinders for consistency.</span>
        </li>
        <li class="flex items-start gap-x-3">
          <span class="text-accent-primary mt-1">⚙️</span>
          <span><strong class="text-coffee-dark">Durability:</strong>&nbsp;Equipment built to last for years.</span>
        </li>
        <li class="flex items-start gap-x-3">
          <span class="text-accent-primary mt-1">⚙️</span>
          <span><strong class="text-coffee-dark">Workflow:</strong>&nbsp;Speed and ease of use in the morning.</span>
        </li>
        <li class="flex items-start gap-x-3">
          <span class="text-accent-primary mt-1">⚙️</span>
          <span><strong class="text-coffee-dark">Maintenance:</strong>&nbsp;Keeping your gear in peak condition.</span>
        </li>
      </ul>
    </div>

    <h3 class="text-2xl font-bold text-coffee-dark mb-8 uppercase tracking-wider">
      Expert Gear Comparisons:
    </h3>

    <div class="grid grid-cols-2 md:grid-cols-4 gap-6">
      {gearFeaturedLinks.map((link) => (
        <a
          href={link.url}
          class="group block bg-white/30 rounded-2xl overflow-hidden transition-all duration-300 hover:-translate-y-2 border border-coffee-brown/10 hover:bg-white/60"
          aria-label={link.title}
        >
          <div class="aspect-[4/3] overflow-hidden border-b border-coffee-brown/5">
            <img
              src={link.image}
              alt={link.title}
              class="w-full h-full object-cover transition-transform duration-700 group-hover:scale-105"
            />
          </div>
          <div class="p-5 text-center">
            <p class="text-sm font-black text-coffee-dark group-hover:text-accent-primary transition-colors uppercase tracking-widest leading-tight">
              {link.title}
            </p>
          </div>
        </a>
      ))}
    </div>

  </div>
</section>